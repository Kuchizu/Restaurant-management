spring:
  application:
    name: billing-service
  cloud:
    config:
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}
      fail-fast: false
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
