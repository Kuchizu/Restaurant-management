# Database credentials
DATABASE_USERNAME=restaurant_user
DATABASE_PASSWORD=restaurant_pass

# JWT Secret (Base64 encoded, minimum 256 bits)
# Generate new one with: openssl rand -base64 64
JWT_SECRET=your_base64_encoded_secret_here
